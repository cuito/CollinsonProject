{
  "name": "collinson-project-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "backend:dev": "yarn workspace backend dev",      
    "frontend:dev": "yarn workspace frontend dev",    
    "dev": "concurrently \"yarn backend:dev\" \"yarn frontend:dev\"" 
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
